name: Main Pipeline

on:
  push:
    branches: [ main ]

jobs:
  orchestrate:
    runs-on: ubuntu-latest
    steps:
      - name: Pipeline Started
        run: |
          echo "🚀 Main pipeline started"
          echo "📦 App workflow will build and push Docker image"
          echo "☸️  Infra workflow will deploy to Minikube"
          echo "⏳ Check individual workflow status in Actions tab"
